# ── Base ───────────────────────────────────────────────────────────────
# Core
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0

# Vision-language model deps
qwen-vl-utils>=0.0.2
Pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# OCR
easyocr>=1.7.1

# Server
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# Schema validation
pydantic>=2.6.0

# ── Optional: alternative backends ─────────────────────────────────────
# Uncomment as needed:
# vllm>=0.4.0                   # Faster inference via vLLM
# auto-gptq>=0.7.0              # GPTQ quantization
# autoawq>=0.2.0                # AWQ quantization
# paddleocr>=2.7.0              # Alternative OCR
# paddlepaddle-gpu>=2.6.0       # PaddleOCR GPU backend

# ── Dev / test ─────────────────────────────────────────────────────────
# pytest>=8.0
# httpx>=0.27.0
# requests>=2.31.0
